/// Numbers are in range [0; 1]
pub struct RGB {
    pub r: f32,
    pub g: f32,
    pub b: f32,
}

/// Numbers are in range [0; 1]
pub struct RGBA {
    pub r: f32,
    pub g: f32,
    pub b: f32,
    pub a: f32,
}
